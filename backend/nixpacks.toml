[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "mkdir -p staticfiles",
  "SECRET_KEY=build-phase-placeholder DJANGO_ALLOW_INSECURE_KEY=1 python manage.py collectstatic --noinput --clear",
]
